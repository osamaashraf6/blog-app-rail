export type TInfoContact = {
  id: number;
  title: string;
  desc: string;
  phone: string;
  email: string;
};
export type TChooseList = {
  id: number;
  img: string;
  title: string;
  desc: string;
  email: string;
};
export type TCardsList = {
  id: number;
  img: string;
  title: string;
  desc: string;
};
export type TRadioInputs = {
  id: number;
  label: string;
};
